<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent" >

    <Button
        android:id="@+id/addInterest"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/interestSpinner"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:onClick="addInterestButtonClicked"
        android:text="Add interest" />

    <Spinner
        android:id="@+id/interestSpinner"
        style="@android:style/Theme.Light"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@+id/addInterest"
        android:text="@string/categories"
        android:textSize="33sp" />

    <ListView
        android:id="@+id/interestList"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/interestSpinner" >

    </ListView>
    
</RelativeLayout>